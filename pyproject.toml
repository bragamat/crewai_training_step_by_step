[project]
name = "app_flow"
version = "0.1.0"
description = "app-flow using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.4.1"
]

[project.scripts]
kickoff = "app_flow.main:kickoff"
run_crew = "app_flow.main:kickoff"
plot = "app_flow.main:plot"
run_with_trigger = "app_flow.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "flow"
